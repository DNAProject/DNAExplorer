import {DEGREEINFO} from '../mutation-types'

const state = {
  degreeInfo: []
}

const mutations = {
  [DEGREEINFO] (state, { data }) {
    state.degreeInfo = data
  }
}

export default {
  state,
  mutations
}
